{
  "name": "weedn-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "cd apps/storefront && npm run dev",
    "build": "cd apps/storefront && npm run build"
  }
}
